/**
 * Site tagline and default SEO copy. Keep in sync with `frontend/index.html`
 * (static crawlers and first paint use the HTML meta tags).
 */
export const SITE_TAGLINE = "Take charge of your health";

export const DEFAULT_PAGE_TITLE = `collabiora | ${SITE_TAGLINE}`;

/** Primary meta / Open Graph / Twitter description for the site. */
export const DEFAULT_META_DESCRIPTION =
  "Take charge of your health with collabiora — plain-language health research, clinical trials, and experts in one place.";
